import { value, onMounted } from "vue-function-api";
function service(props, ctx) {
  
  onMounted(async () => {
    try {
    } catch (error) {
      
    }
  })

  return {  };
}

export default service;